Read @specs/api-contract.md. Design the folder structure for the
FastAPI backend. Follow CLAUDE.md conventions — service layer,
schemas folder, models folder, no business logic in routes.
Use uv for the project setup. Create a pyproject.toml with these
dependencies:
  fastapi, uvicorn[standard], sqlalchemy[asyncio], aiosqlite,
  alembic, pydantic, python-jose[cryptography], httpx, pytest, pytest-asyncio
Do not write any application code yet — just the structure and dependencies.


 Commit these changes. The message should explain this is the initial
FastAPI backend folder structure with uv configuration and dependencies.


Read @specs/api-contract.md and @specs/seed-data.json.

Create the SQLAlchemy 2.0 async Task model in backend/app/models/task.py.
Field names must exactly match the snake_case keys in seed-data.json.
State and assigned_role must be Python Enums.

Create corresponding Pydantic v2 schemas in backend/app/schemas/task.py:
  - TaskCreate (request body for POST)
  - TaskUpdate (request body for PUT, all fields optional)
  - TaskResponse (response body for all endpoints)

Set up the async database engine in backend/app/database.py using
SQLite with aiosqlite. Include a get_db dependency for FastAPI.

Configure alembic/env.py
to work with SQLAlchemy 2.0 async and our SQLite database.
Then generate the first migration for the Task model and apply it.
Use uv to run all alembic commands.


Read @specs/api-contract.md.

Build the four FastAPI endpoints in backend/app/api/v1/tasks.py:
  GET    /api/tasks       — list all tasks
  POST   /api/tasks       — create a task (return 201)
  PUT    /api/tasks/{id}  — update a task
  DELETE /api/tasks/{id}  — delete a task (return 204)

Business logic goes in backend/app/services/task_service.py.
Routes call the service. Service talks to the database.
Routes know nothing about SQLAlchemy. Service knows nothing about HTTP.
Response shapes must match exactly what is in the spec.
Handle errors with HTTPException using the status codes from the spec.
Wire the router into backend/app/main.py with CORS enabled for
http://localhost:8000 origins so the frontend can call it.


Read @specs/seed-data.json. Write a seed script at backend/seed.py
that reads this file and inserts all records into the tasks table
using the async SQLAlchemy session.

The script must be idempotent — if run twice, it should not create
duplicates. Check whether a task with the same id already exists
before inserting.

Run it with uv and confirm the database has five records.


In frontend/app.js, the mockFetch() function currently rejects every
request. Replace the BODY of mockFetch() with a real fetch() call to
http://localhost:8000.

Strict rules:
  - Do NOT change the api.* layer that wraps mockFetch.
  - Do NOT touch any other code in app.js.
  - The function signature, return shape, and call sites stay identical.

Only the inside of mockFetch changes — from a rejecting stub to a
real network call that matches the contract in specs/api-contract.md.




Create a new issue on the content-tracker repo using the GitHub MCP server.

Title: Update specs/api-contract.md with the new auth endpoints

Body: Explain that the three auth endpoints were added but never documented
in the contract file. List them:
  - POST /api/auth/login
  - POST /api/auth/users (admin only)
  - GET /api/auth/me

Note that this should be done before adding more endpoints so the
contract stays the source of truth.

Apply the 'documentation' label.

.


Create a new branch called fix/document-auth-endpoints from main.

On that branch, update specs/api-contract.md to add documentation
for the three auth endpoints — POST /api/auth/login, POST /api/auth/users
(admin only), GET /api/auth/me. Match the existing format in the file.

Commit with a conventional commits style message.
Do NOT push. Do NOT open a PR. Stop after the commit









The branch fix/document-auth-endpoints is now pushed to GitHub.

Using the GitHub MCP server, open a pull request from
fix/document-auth-endpoints into main on the content-tracker repo.

Write a PR description that:
  - Explains what was changed and why
  - References the issue you created earlier (closes it)
  - Notes this completes the contract documentation gap 

Use a conventional commits style title.




We're preparing the frontend for production deployment to Cloud Run.
In frontend/app.js, the backend URL is hardcoded as http://localhost:8000
in two places. Both need to become relative URLs so the same code works
locally AND on Cloud Run.

1. Inside apiFetch: change
   fetch(`http://localhost:8000${url}`, ...)
   to
   fetch(url, ...)

2. Inside handleLoginSubmit (which does NOT use apiFetch — the login
   form calls fetch directly): change
   fetch('http://localhost:8000/api/auth/login', ...)
   to
   fetch('/api/auth/login', ...)

Show me the diff before applying. Also scan the file and tell me if
there are any OTHER localhost references — comments, debug logs,
anywhere — that I might have missed.



We need FastAPI to serve our frontend as static files so the entire
app runs from a single origin in production.

In backend/app/main.py (NOT backend/main.py — that's just a stub):

1. Add an import at the top:
   from fastapi.staticfiles import StaticFiles

2. AFTER both router includes (auth_router and tasks_router), add a
   catch-all static files mount. The directory must be "../frontend"
   because when we deploy via Docker the WORKDIR will be /app/backend
   and the frontend folder gets copied to /app/frontend (one level up).

   app.mount("/", StaticFiles(directory="../frontend", html=True), name="static")

Critical: the mount MUST come AFTER both routers. FastAPI checks routes
in registration order — if the catch-all static mount comes first, it
will intercept every request including /api/* and break the API.

Show me the diff. Confirm the mount is positioned after both routers.
Do not touch any existing code — only add the import and the mount.




Write a Dockerfile at the project root for the Telusko Workflow Engine.

Project structure:
- Python 3.12 (pyproject.toml requires Python 3.12 or higher)
- Backend at backend/ with app/, seed.py, alembic/, alembic.ini, pyproject.toml
- Frontend at frontend/ (vanilla HTML/CSS/JS, no build step)
- Specs at specs/ — REQUIRED at runtime because backend/seed.py reads
  Path(__file__).parent.parent / "specs" / "seed-data.json"
- backend/app/main.py is the real FastAPI app (backend/main.py is just a stub)
- backend/app/main.py mounts StaticFiles from directory "../frontend"
- alembic.ini lives in backend/; alembic upgrade head must run from backend/

Dockerfile requirements:
- Base image: python:3.12-slim
- Package manager: uv
- WORKDIR /app/backend so all relative paths in the code resolve correctly
- Optimise layer caching — copy pyproject.toml and install deps BEFORE
  copying application code
- Copy backend/app, backend/seed.py, backend/alembic, backend/alembic.ini
- Copy specs/ to ../specs (one level up from WORKDIR — required by seed.py)
- Copy frontend/ to ../frontend (one level up — served by StaticFiles)
- CMD: alembic upgrade head, then python seed.py, then uvicorn
- Bind to 0.0.0.0 on $PORT (default 8080; Cloud Run sets this env var)
- Add a comment on every line explaining what it does



telusko-app:latest -> docker image

Project ID - telusko-content-tracker

repo - telusko-repo

docker run -p 8080:8080 `
  -e JWT_SECRET_KEY='local-test-secret-not-for-production' `
  -e JWT_ALGORITHM='HS256' `
  -e JWT_ACCESS_TOKEN_EXPIRE_MINUTES='60' `
  telusko-app



gcloud builds submit `
  --tag us-central1-docker.pkg.dev/telusko-content-tracker/telusko-repo/telusko-app:latest


gcloud run deploy telusko-app `
  --image us-central1-docker.pkg.dev/telusko-content-tracker/telusko-repo/telusko-app:latest `
  --platform managed `
  --region us-central1 `
  --allow-unauthenticated `
  --port 8080 `
  --set-env-vars "JWT_SECRET_KEY=LmCiO2etexr8bdRA,JWT_ALGORITHM=HS256,JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60